before_script:
  - export GOPATH=$HOME/go
  - ln -svf $CI_PROJECT_DIR $GOPATH/src/$CI_PROJECT_DIR
  - cd $GOPATH/src/$REPO_NAME

stages:
  - build

compile:
  stage: build
  script:
    - go build -race
